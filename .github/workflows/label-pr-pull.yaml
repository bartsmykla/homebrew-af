name: "add pr-pull label"

on:
  workflow_run:
    workflows:
    - foo
    types:
    - completed

jobs:
  add-label:
    runs-on: ubuntu-24.04

    steps:
    - name: "Print GitHub context"
      run: |
        cat <<EOF
        ${{ toJSON(github) }}
        EOF
